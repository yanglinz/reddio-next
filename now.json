{
  "version": 2,
  "builds": [
    { "src": "build/*", "use": "@now/static" },
    { "src": "server/*", "use": "@now/node" }
  ],
  "routes": [
    {
      "src": "/graphql/(.*)",
      "dest": "server/index.ts"
    },
    { "src": "/(.*)", "dest": "build/index.html" }
  ]
}
